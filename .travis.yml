sudo: required
services:
  - docker

before_install:
  - docker build -t sherrajput/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sherrajput/react-test npm test

after_success:
  - docker build -t sherrajput/multi-client ./client
  - docker build -t sherrajput/multi-nginx ./nginx
  - docker build -t sherrajput/multi-server ./server
  - docker build -t sherrajput/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sherrajput/multi-client
  - docker push sherrajput/multi-nginx
  - docker push sherrajput/multi-server
  - docker push sherrajput/multi-worker

